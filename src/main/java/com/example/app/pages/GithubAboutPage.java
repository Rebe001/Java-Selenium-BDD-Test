package com.example.app.pages;

import com.example.app.common.Helper;
import org.openqa.selenium.WebDriver;

public class GithubAboutPage extends Helper {

    public GithubAboutPage(WebDriver driver) { super(driver); }
}
